import authAPI from "./endpoints/auth-api";
import followAPI from "./endpoints/follow-api";
import profileAPI from "./endpoints/profile-api";
import usersAPI from "./endpoints/users-api";

const API = {
  usersAPI,
  followAPI,
  authAPI,
  profileAPI,
};

export default API;
